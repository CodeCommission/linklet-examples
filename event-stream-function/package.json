{
  "name": "event-stream-function",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "start": "linklet serve",
    "dev": "nodemon --exec linklet serve -p 3000",
    "test": "mocha --require async-to-gen/register index.test.js",
    "deploy": "dropstack deploy --url https://api.cloud.dropstack.run"
  },
  "engines": {
    "node": "^6"
  },
  "dependencies": {
    "linklet": "latest"
  },
  "license": "MIT",
  "devDependencies": {
    "eventsource": "^1.0.1",
    "mocha": "^3.4.2",
    "nodemon": "^1.11.0"
  }
}
